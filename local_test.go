package chotki

import (
	"testing"
)

func TestChotki_ParseEvent(t *testing.T) {
	/*_ = os.RemoveAll("test")
	var repl Chotki
	err := repl.Open(123)
	assert.Nil(t, err)
	so := repl.NewObject()
	assert.Equal(t, 0, so.Sum())
	so.Add(1)
	assert.Equal(t, 1, so.Sum())
	so.Add(2)
	assert.Equal(t, 3, so.Sum())
	err = repl.Close()
	assert.Nil(t, err)

	var ch2 Chotki
	err = repl.Open("test")
	assert.Nil(t, err)
	soso, err := repl.FindObject(so.Id())
	assert.Nil(t, err)
	assert.Equal(t, 3, soso.Sum())
	err = ch2.Close()
	assert.Nil(t, err)*/
}
