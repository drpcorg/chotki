package main

import (
	"testing"
)

func TestChotki_ParseEvent(t *testing.T) {
	/*_ = os.RemoveAll("test")
	var ch Chotki
	err := ch.Open(123)
	assert.Nil(t, err)
	so := ch.CreateObject()
	assert.Equal(t, 0, so.Sum())
	so.Add(1)
	assert.Equal(t, 1, so.Sum())
	so.Add(2)
	assert.Equal(t, 3, so.Sum())
	err = ch.Close()
	assert.Nil(t, err)

	var ch2 Chotki
	err = ch.Open("test")
	assert.Nil(t, err)
	soso, err := ch.FindObject(so.Id())
	assert.Nil(t, err)
	assert.Equal(t, 3, soso.Sum())
	err = ch2.Close()
	assert.Nil(t, err)*/
}
