package chotki

import (
	"testing"
)

func TestChotki_ParseEvent(t *testing.T) {
	/*_ = os.RemoveAll("test")
	var Host Chotki
	err := Host.Open(123)
	assert.Nil(t, err)
	so := Host.CreateObject()
	assert.Equal(t, 0, so.Sum())
	so.Add(1)
	assert.Equal(t, 1, so.Sum())
	so.Add(2)
	assert.Equal(t, 3, so.Sum())
	err = Host.Close()
	assert.Nil(t, err)

	var ch2 Chotki
	err = Host.Open("test")
	assert.Nil(t, err)
	soso, err := Host.FindObject(so.Id())
	assert.Nil(t, err)
	assert.Equal(t, 3, soso.Sum())
	err = ch2.Close()
	assert.Nil(t, err)*/
}
